class Solution:
    def isPalindrome(self, x: int) -> bool:
        a=str(x)
        b=str(x)
        a=a[ : :-1]
        if(a==b):
            return True
        
        else:
            return False
            
