class Solution:
    def isPalindrome(self, s: str) -> bool:
        s=''.join(e for e in s if e.isalnum())
        s=s.lower()
        a= s[ : :-1]
        if (a==s):
            return True 
        
        else :
            return False 
    
